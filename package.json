{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "cd public && python3 -m http.server 5173",
    "build": "rm -rf dist && cp -r public dist",
    "preview": "cd dist && python3 -m http.server 4173",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "@netlify/functions": "^5.0.1",
    "netlify-cli": "^23.9.5",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
